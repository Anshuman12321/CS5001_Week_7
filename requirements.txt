# Combined deps for MCP (`mcp/`) + A2A (`a2a/`) — install once from repo root:
#   pip install -r requirements.txt

mcp>=1.0.0
fastapi>=0.111
pydantic>=2.0
httpx>=0.27
click>=8.1
rich>=13.0
python-dotenv>=1.0
uvicorn[standard]>=0.29
